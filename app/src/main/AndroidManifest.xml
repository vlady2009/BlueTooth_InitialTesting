<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.fadstoobsessions.bluetooth">

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_mylauncher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.fadstoobsessions.bluetooth.SplashScreen"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.fadstoobsessions.bluetooth.DeviceList"
            android:label="@string/app_name">

        </activity>

        <activity
            android:name="com.fadstoobsessions.bluetooth.ledControl"
            android:label="LED Control"
            android:windowSoftInputMode="stateHidden">

        </activity>

    </application>

</manifest>
